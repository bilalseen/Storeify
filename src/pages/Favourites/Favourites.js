import { View, Text } from "react-native";
import React from "react";
import styles from "./Favourites.style";

const Favourites = () => {
  return (
    <View style={styles.mainContainer}>
      <Text>Favourites Pageee!</Text>
    </View>
  );
};

export default Favourites;
