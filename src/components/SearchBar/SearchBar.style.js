import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  input: {
    width: 300,
    paddingVertical: 10,
    paddingHorizontal: 20,
    borderRadius: 10,
    borderColor: "black",
    borderWidth: 1
  },
});
